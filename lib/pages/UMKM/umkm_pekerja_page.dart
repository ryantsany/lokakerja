import 'package:flutter/material.dart';

class PekerjaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pekerja"),
      ),
      body: Center(
        child: Text(" halaman Pekerja", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}